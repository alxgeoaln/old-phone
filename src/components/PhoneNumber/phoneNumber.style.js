import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    phoneNumberStyle: {
        fontSize: 35,
        fontWeight: 'bold',
        color: '#1E1E2C',
        position: 'absolute',
        bottom: 30
    }
});

export default styles;